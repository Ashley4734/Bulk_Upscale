# Core dependencies
Pillow>=10.0.0

# AI Upscaling dependencies (optional, only needed for --ai mode)
# Install with: pip install -r requirements.txt
realesrgan>=0.3.0
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
