# AI Upscaling Dependencies for Real-ESRGAN
# Compatible versions of PyTorch and torchvision for basicsr 1.4.2 on Python 3.11+

# Core PyTorch packages (tested compatible versions)
# torch 2.9.x ships wheels for Python 3.11–3.13 while remaining compatible with
# basicsr 1.4.2 and realesrgan 0.3.x. torchvision 0.24.1 matches torch 2.9.1 and
# provides Python 3.13 wheels, avoiding install failures on newer interpreters.
torch==2.9.1
torchvision==0.24.1

# Real-ESRGAN and dependencies
realesrgan==0.3.0
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8

# Additional dependencies
opencv-python>=4.5.0
numpy<2.0.0
Pillow>=8.0.0
